# Workspace User

> Replace every `<...>` value with local configuration. Remove entries that do not apply.

## Git Branch Namespace

Use `<username-or-namespace>` as the username component in `<username>/<feature>` branch names.

## Agent Commit Commands

Use the command matching the active agent harness:

- `<agent-or-harness>`: `<configured-git-commit-command>`
- `<another-agent-or-harness>`: `<configured-git-commit-command>`

Document any required signing, sign-off, or attribution behavior here.
